let groceryList = ['Arroz', 'Feijão', 'Alface', 'Melancia'];
for (let index = 0; index < groceryList.length; index++) {
    let mensagem = "Compre imediatamente isso aqui:" + groceryList[index];
    console.log(mensagem)
}
